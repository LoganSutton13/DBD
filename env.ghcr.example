# Copy to .env next to docker-compose.ghcr.yml and adjust.
# Registry namespace = GitHub user or org (lowercase), same as in ghcr.io/<namespace>/...

DBD_IMAGE_REGISTRY=ghcr.io/your-github-user-or-org
DBD_VERSION=latest
